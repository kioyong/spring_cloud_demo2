server:
  port: 9005
  context-path: /location
spring:
  application:
      name: location
  data:
    mongodb:
      uri: mongodb://localhost:27017/demo
      database: demo
logging:
  level:
    com.yong: DEBUG
    org.springframework.cloud.config.client: DEBUG
management:
  security:
    enabled: false