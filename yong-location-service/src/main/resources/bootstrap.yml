server:
  port: 9005
  servlet:
    context-path: /location

spring:
  application:
      name: location
  data:
    mongodb:
      uri: mongodb://localhost:27017/demo
      database: demo

management:
  security:
    enabled: false

logging:
  level:
    com.yong: DEBUG
    org.springframework.cloud.config.client: DEBUG